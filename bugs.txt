1-Tambien hay un problema y es que en la seccion de create/edit lienzos en el dropdown de seleccionar paleta de colores tiene el btn de crear paleta de colores, el problema es que esta mal localizado el btn ya que 1-el btn debe medir el 100% ancho disponible, 2-si te fijas en el modulo de languaje tenemos un input search en la parte de arriba copia ese mismo layout pero en lugar de input de busqueda pon el btn de crear paleta personalizada, pero inviertelo y pasalo a la parte de abajo, recuerda sin estilos inline, solo clases/modificadores, ah y pues modificador para que el borde este en tl top y no en bottom
